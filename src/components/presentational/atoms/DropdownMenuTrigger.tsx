import { DropdownMenuTrigger as ShadcnDropdownMenuTrigger } from "../../ui/dropdown-menu";

export default function DropdownMenuTrigger({
  children,
}: {
  children: React.ReactNode;
}) {
  return <ShadcnDropdownMenuTrigger>{children}</ShadcnDropdownMenuTrigger>;
}
